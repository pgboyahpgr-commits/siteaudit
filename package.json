{
  "name": "siteaudit",
  "version": "0.1.0",
  "private": true,
  "description": "SiteAudit - Free web security scanner & reverse-engineering agent. Paste a URL, get a full vulnerability report with fixes.",
  "workspaces": [
    "web",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c green,cyan \"npm run dev -w server\" \"npm run dev -w web\"",
    "dev:server": "npm run dev -w server",
    "dev:web": "npm run dev -w web",
    "build": "npm run build -w web",
    "start": "npm run start -w server",
    "lint": "echo \"No lint configured yet\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=22.5"
  },
  "allowScripts": {
    "esbuild@0.21.5": true
  }
}
